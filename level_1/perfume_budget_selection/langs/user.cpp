#include <iostream>
#include <vector>

class Node {
public:
    int key;
    Node* left;
    Node* right;

    Node(int value) : key(value), left(nullptr), right(nullptr) {}
};

std::vector<int> find_closest_perfumes(Node* root, int x) {
    //Code here
    return ;
}