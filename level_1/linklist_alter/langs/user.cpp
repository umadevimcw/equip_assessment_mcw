#include <iostream>
#include <vector>
using namespace std;

struct Node {
    int data;
    Node* next;
    Node(int val) : data(val), next(nullptr) {}
};

// Function to split the linked list into two with alternate nodes
vector<Node*> splitAlternateNodes(Node* head) {
    //Code here
    return {, };
}

