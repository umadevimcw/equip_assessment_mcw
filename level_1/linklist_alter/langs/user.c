// Function to split the linked list into two with alternate nodes
struct Node** splitAlternateNodes(struct Node* head) {
    
    // Code here
    // return the code in the following way
    struct Node** result = (struct Node**)malloc(2 * sizeof(struct Node*));
    //result[0] = first link list;
    //result[1] = second link list;
    return result;
}
